packages:
  - "."

overrides:
  "next>postcss": ^8.5.10
  "fumadocs-mdx>esbuild": ^0.28.1

# Explicit supply-chain policy: docs type generation does not require package
# lifecycle scripts, so keep both native dependencies disabled by default.
allowBuilds:
  esbuild: false
  sharp: false
